# Build artifacts
bin/
*.out
*.o

# Root-level compiled binaries (lab outputs built locally)
/demand_paging
/page_cache_models
/pm_exercises
/lab_*

# macOS
.DS_Store
.DS_Store?
._*

# Logs and test data
*.log
testdata.bin

# Archives (kept locally for reference, not needed in repo)
*.tar.gz
*.tar
*.zip
